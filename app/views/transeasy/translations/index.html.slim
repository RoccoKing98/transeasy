h1.page-title Transeasy Home

= form_with model: @translation_files, url: save_setup_translations_path, method: :post do |f|
  - if @translation_files
    - @translation_files.each do |tf|
      p =  link_to "Edit Translation File : #{tf[:file_name]}", edit_translation_file_translations_path(id: tf), class: 'btn btn-info'

  p
  = link_to "Import Translation File", import_translation_file_translations_path, class: 'btn btn-primary'
  p
  = link_to "Create Translation File", edit_translation_file_translations_path, class: 'btn btn-primary'

